{
  "name": "stock-management",
  "version": "1.0.0",
  "private": true,
  "description": "Gestion des Achats & Approvisionnements — backend Express/Prisma/PostgreSQL + frontend React/Vite",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "db:migrate": "npm --prefix backend run db:migrate",
    "db:seed": "npm --prefix backend run db:seed",
    "dev": "concurrently -n api,web -c blue,green \"npm --prefix backend run dev\" \"npm --prefix frontend run dev\"",
    "build": "npm --prefix backend run build && npm --prefix frontend run build",
    "start": "cross-env NODE_ENV=production npm --prefix backend run start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3"
  }
}
